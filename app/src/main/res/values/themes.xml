<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AssigmentApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AssigmentApp" parent="Base.Theme.AssigmentApp" />

    <!--
      ADD THIS NEW THEME FOR THE SPLASH SCREEN
    -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background color -->
        <item name="windowSplashScreenBackground">@color/white</item>

        <!-- Set the icon to display on the splash screen -->
        <!-- Replace 'your_logo_name' with the actual name of your logo file in res/drawable -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/img_2</item>

        <!-- Set the theme to be used after the splash screen is gone -->
        <item name="postSplashScreenTheme">@style/Theme.AssigmentApp</item>
    </style>

</resources>